{
  "name": "vsc-or",
  "displayName": "OR",
  "description": "Receive execution bundles and submit evidence from your IDE",
  "version": "0.1.4",
  "publisher": "raftio",
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:or.bundleTree",
    "onView:or.chatView"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "or",
          "title": "OR",
          "icon": "$(symbol-structure)"
        }
      ]
    },
    "views": {
      "or": [
        {
          "id": "or.bundleTree",
          "name": "Execution Bundle"
        },
        {
          "id": "or.chatView",
          "name": "Chat",
          "type": "webview"
        }
      ]
    },
    "commands": [
      {
        "command": "or.login",
        "title": "OR: Sign In",
        "icon": "$(sign-in)"
      },
      {
        "command": "or.logout",
        "title": "OR: Sign Out",
        "icon": "$(sign-out)"
      },
      {
        "command": "or.fetchBundle",
        "title": "OR: Fetch Bundle",
        "icon": "$(cloud-download)"
      },
      {
        "command": "or.submitEvidence",
        "title": "OR: Submit Evidence",
        "icon": "$(beaker)"
      },
      {
        "command": "or.disconnect",
        "title": "OR: Disconnect"
      },
      {
        "command": "or.openChat",
        "title": "OR: Open Chat",
        "icon": "$(comment-discussion)"
      },
      {
        "command": "or.refreshBundle",
        "title": "Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "or.executeTask",
        "title": "OR: Execute Task",
        "icon": "$(play)"
      },
      {
        "command": "or.executeBundle",
        "title": "OR: Execute Bundle",
        "icon": "$(run-all)"
      }
    ],
    "viewsWelcome": [
      {
        "view": "or.bundleTree",
        "contents": "Sign in to OR to view your execution bundles.\n[Sign In](command:or.login)",
        "when": "!or.loggedIn"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "or.fetchBundle",
          "when": "view == or.bundleTree && or.loggedIn",
          "group": "navigation@1"
        },
        {
          "command": "or.refreshBundle",
          "when": "view == or.bundleTree && or.loggedIn",
          "group": "navigation@2"
        },
        {
          "command": "or.logout",
          "when": "view == or.bundleTree && or.loggedIn",
          "group": "navigation@3"
        }
      ],
      "view/item/context": [
        {
          "command": "or.executeBundle",
          "when": "view == or.bundleTree && viewItem == bundle",
          "group": "inline"
        },
        {
          "command": "or.executeTask",
          "when": "view == or.bundleTree && viewItem == task",
          "group": "inline"
        }
      ]
    },
    "configuration": {
      "title": "OR",
      "properties": {
        "or.apiUrl": {
          "type": "string",
          "default": "http://localhost:3001",
          "description": "Base URL of the OR API"
        }
      }
    }
  },
  "scripts": {
    "build": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node --sourcemap",
    "watch": "npm run build -- --watch",
    "package": "npm run build -- --minify",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.24.0",
    "eslint": "^9.0.0",
    "typescript": "^5.6.0"
  }
}
